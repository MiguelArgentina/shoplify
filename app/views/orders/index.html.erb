<div class="container-fluid">
<h2>All Orders</h2>
<br>
<br>
<% @orders.each do |order| %>
  <h5>Order Nº: <%= order.id %></h5>
  <h6>Date: <%= order.created_at.strftime("%B %-d, %Y. %H:%M hs") %></h6>
  <h6>Products: </h6>
  <ul>
    <% order.line_items.each do |item| %>
    <li><%= item.quantity %> x <%= item.product.name %></li>
    <% end %>
  </ul>
  <hr>
<% end %>

</div>